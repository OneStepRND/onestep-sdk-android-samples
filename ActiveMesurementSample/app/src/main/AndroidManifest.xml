<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Runtime ("dangerous") permission the host app requests (see MainScreen): required for
         step/movement detection during a recording. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- The OneStep SDK contributes its other required permissions via manifest merging, so you do
         not declare them yourself. Some are privacy-sensitive (e.g. microphone/audio and location
         used for motion sensing) and must be reflected in your Play Store Data safety form. Open
         Android Studio → Merged Manifest for the authoritative list. -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OneStepSDKSampleApp"
        android:name=".SDKSampleApplication"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OneStepSDKSampleApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>